# Supabase プロジェクトの URL（例: https://xxxxxxxxxxxx.supabase.co）
SUPABASE_URL=

# Supabase の service_role キー。
# RLS をバイパスするため、サーバーサイド以外へ絶対に露出させないこと。
SUPABASE_SERVICE_ROLE_KEY=

# MCP サーバーの固定認証トークン。
# クライアントは Authorization: Bearer <MCP_AUTH_TOKEN> ヘッダで送信する。
# 生成例: openssl rand -base64 48
MCP_AUTH_TOKEN=
